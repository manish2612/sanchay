import { createFileRoute } from '@tanstack/react-router';
import Page from '../../../../../app/accounts/masters/voucher-type/page';

export const Route = createFileRoute('/accounts/masters/voucher-type/')({
  component: Page,
});
