{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "refresh": "turbo run build --filter=@sanchay/design-tokens && turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "refresh:web": "turbo run build --filter=@sanchay/design-tokens && turbo run dev --filter=web",
    "dev:mobile": "pnpm --filter mobile dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.26.1",
  "name": "sanchay",
  "dependencies": {
    "next-themes": "^0.2.1"
  }
}